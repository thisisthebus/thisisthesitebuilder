from setuptools import setup

setup(
   name='thisisthesitebuilder',
   version='1.0',
   description='Another Opinionated Static Site Builder',
   author='jMyles',
   author_email='justin@justinholmes.com',
   packages=['thisisthesitebuilder'],  # would be the same as name
#    install_requires=['wheel', 'bar', 'greek'], #external packages acting as dependencies
)
